<%= form_for @wardrobe, url: @wardrobe.new_record? ? admin_wardrobes_path : admin_wardrobe_path(@wardrobe) do |f| %>
  <% if @wardrobe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wardrobe.errors.count, "error") %> prohibited this wardrobe from being saved:</h2>

      <ul>
      <% @wardrobe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :clothing_id %><br>
    <%= f.collection_select :clothing_id, Clothing.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.collection_select :user_id, User.all, :id, :email %>
  </div>
  <div class="field">
    <%= f.label :chosen %><br>
    <%= f.number_field :chosen %>
  </div>
  <div class="field">
    <%= f.label :recommended %><br>
    <%= f.number_field :recommended %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
